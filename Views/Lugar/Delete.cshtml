@model SistemaGestionEventos.Models.LugarModel

@{
    ViewData["Title"] = "Eliminar Lugar";
}

<section class="crud-shell">
    <div class="page-heading">
        <h1>Eliminar Lugar</h1>
        <a asp-action="Index" class="btn btn-outline-secondary">Volver</a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <dl class="row mb-0">
            <dt class="col-sm-4">Nombre</dt>
            <dd class="col-sm-8">@Model.Nombre</dd>
            <dt class="col-sm-4">Ciudad</dt>
            <dd class="col-sm-8">@Model.Ciudad</dd>
            <dt class="col-sm-4">Capacidad</dt>
            <dd class="col-sm-8">@Model.Capacidad</dd>
            <dt class="col-sm-4">Tipo</dt>
            <dd class="col-sm-8">@Model.Tipo</dd>
            </dl>
        </div>
    </div>
    <div class="mt-3 d-flex gap-2">
        <form asp-action="Delete"><input type="hidden" asp-for="LugarId" /><button type="submit" class="btn btn-danger">Eliminar</button></form>
    </div>
</section>
